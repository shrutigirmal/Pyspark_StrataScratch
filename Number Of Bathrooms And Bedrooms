# Import your libraries
import pyspark

from pyspark.sql import functions as F

# Start writing code
avg=airbnb_search_details.groupBy("city","property_type").agg(F.avg("bathrooms").alias("bath"), F.avg("bedrooms").alias("bed"))

# To validate your solution, convert your final pySpark df to a pandas df
avg.toPandas()
